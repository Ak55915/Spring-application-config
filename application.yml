spring:
 datasource:
  url: jdbc:mysql://localhost:3306/micro_yt
 datasource:
  username: root
 datasource:
  password: root

spring:
 jpa: 
  show-sql: true
  
spring:
 jpa:
  hibernate:
   ddl-auto: update

eureka:
 client: 
  service-url:
   defaultZone: http://localhost:8084/eureka/
